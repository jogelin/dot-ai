/**
 * @dot-ai/claude — Claude Code adapter
 *
 * Provides dot-ai workspace integration via Claude Code hooks.
 * The actual boot logic is in hooks/hooks.json (prompt-based).
 * This module exists for future programmatic integration.
 */
export const version = "0.3.0";
